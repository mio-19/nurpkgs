diff --git a/lib/main.dart b/lib/main.dart
index 912287b..195e681 100644
--- a/lib/main.dart
+++ b/lib/main.dart
@@ -275,8 +275,9 @@ class MyHomePageState extends State<MyHomePage> {
     } on TimeoutException {
       await Future.delayed(const Duration(milliseconds: 600));
       setState(() {
+        var localizations = AppLocalizations.of(context);
         weatherError = WeatherError(
-          errorTitle: AppLocalizations.of(context)!.weakOrNoWifiConnection,
+          errorTitle: localizations?.weakOrNoWifiConnection ?? "Weak or no wifi connection",
           errorIcon: Icons.signal_wifi_0_bar,
           location: location,
           latLon: latLon,
@@ -300,8 +301,9 @@ class MyHomePageState extends State<MyHomePage> {
     } on SocketException {
       await Future.delayed(const Duration(milliseconds: 600));
       setState(() {
+        var localizations = AppLocalizations.of(context);
         weatherError = WeatherError(
-          errorTitle: AppLocalizations.of(context)!.notConnectedToTheInternet,
+          errorTitle: localizations?.notConnectedToTheInternet ?? "Not connected to the internet",
           errorIcon: Icons.wifi_off_rounded,
           location: location,
           latLon: latLon,
@@ -310,6 +312,8 @@ class MyHomePageState extends State<MyHomePage> {
       });
     }
     catch (e, stacktrace) {
+      print("General Error catch: $e");
+      print(stacktrace);
       await Future.delayed(const Duration(milliseconds: 600));
       if (kDebugMode) {
         debugPrint('Stack trace: $stacktrace');
